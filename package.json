{
  "name": "toshstack",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev:server": "pnpm --filter server start:dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm -r build"
  }
}
